# TODO
- [x] Update `contact.html` to demo website content (keep existing header/nav styling and demo footer copy)
- [x] Verify required form field IDs still match `sendForm()` logic
- [ ] Smoke test by opening `contact.html` in a browser



